aloha
In this assignment, I learned the basic workflow of Git and GitHub,
including cloning a repository, creating commits, basics like git add/clone/commit/push, creating and switching branches, merging branches(branch and merge), checking out previous commits, and using reflog.
I also learned how to use a pretrained ResNet model from Hugging Face
to run inference on the MNIST dataset and preprocess images to fit the
input size of the model.
